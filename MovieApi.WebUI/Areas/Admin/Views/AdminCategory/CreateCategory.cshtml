@model AdminCreateCategoryDto

@{
    ViewData["Title"] = "CreateCategory";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<style>
    :root {
        --primary: #6366f1;
        --text-dark: #1f2937;
        --text-muted: #6b7280;
        --bg-soft: #f8fafc;
        --radius: 12px;
    }

    .admin-card {
        max-width: 520px;
        margin: 40px auto;
        background: #fff;
        border-radius: var(--radius);
        padding: 28px;
        box-shadow: 0 10px 28px rgba(0,0,0,.05);
    }

    .form-title {
        font-size: 1.4rem;
        font-weight: 700;
        color: var(--text-dark);
        margin-bottom: 6px;
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .form-subtitle {
        font-size: 0.9rem;
        color: var(--text-muted);
        margin-bottom: 24px;
    }

    .form-group label {
        font-weight: 600;
        font-size: 0.85rem;
        color: var(--text-dark);
        margin-bottom: 6px;
        display: block;
    }

    .form-control-modern {
        width: 100%;
        padding: 12px 14px;
        border-radius: var(--radius);
        border: 1px solid #e5e7eb;
        background: var(--bg-soft);
        font-size: 0.95rem;
    }

        .form-control-modern:focus {
            outline: none;
            border-color: var(--primary);
            background: #fff;
            box-shadow: 0 0 0 3px rgba(99,102,241,.15);
        }

    .form-actions {
        display: flex;
        justify-content: flex-end;
        gap: 10px;
        margin-top: 24px;
    }

    .btn-primary-soft {
        background: var(--primary);
        color: #fff;
        padding: 10px 18px;
        border-radius: var(--radius);
        font-weight: 600;
        border: none;
        display: inline-flex;
        align-items: center;
        gap: 6px;
    }

    .btn-secondary-soft {
        background: #e5e7eb;
        color: #374151;
        padding: 10px 18px;
        border-radius: var(--radius);
        font-weight: 600;
        text-decoration: none;
    }
</style>

<div class="admin-card">
    <div class="form-title">
        🗂️ Yeni Kategori Ekle
    </div>
    <div class="form-subtitle">
        Sisteme yeni bir kategori eklemek için aşağıdaki alanı doldurun.
    </div>

    <form method="post">
        <div class="form-group">
            <label>Kategori Adı</label>
            <input asp-for="CategoryName"
                   class="form-control-modern"
                   placeholder="Örn: Dram, Bilim Kurgu, Komedi" />
        </div>

        <div class="form-actions">
            <a href="/Admin/AdminCategory/CategoryList" class="btn-secondary-soft">
                Vazgeç
            </a>
            <button type="submit" class="btn-primary-soft">
                <ion-icon name="save-outline"></ion-icon>
                Kaydet
            </button>
        </div>
    </form>
</div>

